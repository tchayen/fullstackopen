{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "buildui": "rm -rf ./build;cd ../phonebook;yarn build;cp -r build ../backend",
    "deploy": "git subtree push --prefix part3/backend heroku master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tomasz Czajęcki",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.4"
  }
}
